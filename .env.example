DATABASE_URL=postgresql+psycopg2://postgres:CHANGE_ME@localhost:5432/eye_fatigue
APP_HOST=127.0.0.1
APP_PORT=8000
# Set 0 if the browser should not open automatically when running app.py.
OPEN_BROWSER=1

# Opsional: isi agar endpoint laporan PDF dan CSV wajib memakai token.
# Token dapat dikirim melalui header X-Report-Token atau query ?access_key=TOKEN_INI.
REPORT_ACCESS_TOKEN=

